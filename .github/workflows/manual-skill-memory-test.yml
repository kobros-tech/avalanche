name: Skill Memory manual test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Skill Memory tests
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          python -m unittest discover tests
